# Parallels X — Prototype 2.7B

## Living Training Road

This patch turns the space between Rrvvfo's Sage refresher and the tournament into a playable 3D Chapter 1 route.

Install with `install.sh`. The installer backs up the current branch, resets to `origin/main`, applies the patch, runs syntax and module-graph checks, commits, and pushes.
