# Parallels X — Prototype 2.7B.1

Corrective update for the Chapter 1 Training Grounds and Tournament Road.

Install over the current `main` branch. The installer creates a backup branch, preserves uncommitted work in a stash, validates the changed module graph, commits, and pushes.
