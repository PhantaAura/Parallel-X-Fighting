# Prototype 2.4C Stage Select Hotfix

## Fixed

- Tangai Dojo is always derived as playable from the loaded stage registry.
- Global Tournament remains playable.
- Asrylyte Zone remains honestly marked as coming next.
- Arena cards are rebuilt every time the Arena Select screen opens.
- Added cache-busting for both `arena-mode.js` and `arena-stages.js` to prevent Safari from reusing stale availability data.
- The arena badge now reads `PROTOTYPE 2.4C • STAGE FIX` so the loaded patch is easy to verify.
